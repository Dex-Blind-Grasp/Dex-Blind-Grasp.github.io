---
import { Icon } from "astro-icon/components";
import type { Link } from "../types/types";

interface Props {
  links: Link[];
}

const { links } = Astro.props;
---

<div class="not-prose flex flex-row flex-wrap justify-center gap-3">
  {
    links.map((link) => (
      <a
        href={link.url}
        class="project-link flex flex-row items-center gap-2 rounded-full px-5 py-2 text-base font-semibold hover:no-underline"
      >
        {link.icon && <Icon name={link.icon} />}
        <span>{link.name}</span>
      </a>
    ))
  }
</div>
